<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <meta name="description" content="{{meta_description}}">

    <title>{{meta_title}}</title>

    <link rel="stylesheet" href="{{asset "css/style.css"}}">
    <link href='https://fonts.googleapis.com/css?family=Roboto:300,300italic,400,700italic,700,400italic' rel='stylesheet' type='text/css'>

    {{ghost_head}}
</head>
<body class="{{body_class}}">
    {{navigation}}

    {{{body}}}

    <footer class="gr-footer">
        <section class="gr-footer__brand">
            <div class="gr-footer__brand-logo">
                <a href="{{@blog.url}}">
                    {{#if @blog.logo}}
                        <img src="{{@blog.logo}}" alt="{{@blog.title}}" title="{{@blog.title}}">
                    {{else}}
                        {{@blog.title}}
                    {{/if}}
                </a>
            </div>
            <div class="gr-footer__brand-nav">
                {{navigation}}
            </div>
            <div class="gr-footer__brand-social">
                Social
            </div>
        </section>
        <hr>
        <section class="gr-footer__copyright">
            <strong>&copy; {{date format="YYYY"}} <a href="{{@blog.url}}">{{@blog.title}}</a>.</strong><br>
            Proudly published with <a href="https://ghost.org">Ghost</a>
        </section>
    </footer>

    {{ghost_foot}}
</body>
</html>
